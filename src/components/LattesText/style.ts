import styled from 'styled-components';

export const FormattedLattes = styled.div`
  font-size: 0.8rem;
  min-width: 150px;
`;

export const CopyPopup = styled.div`
  width: 5.2em;
  height: 1.3em;
  padding: 0.4em;
  border-radius: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: rgb(28, 28, 28, 0.7);
  font-size: 0.78rem;
  font-weight: 400;
  color: #ffffff;
  position: absolute;
  left: 90%;
`;
